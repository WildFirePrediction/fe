export type LocationCoord = {
  lat: number;
  lon: number;
};

export type FullCoord = {
  latitude: number;
  longitude: number;
};

export type FullCoordWithName = {
  name: string;
  latitude: number;
  longitude: number;
};
